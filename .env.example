# API (required by src/services/api.ts)
# Use full URL including scheme, e.g. https://api.example.com
NEXT_PUBLIC_API_BASE_URL=
# Optional: server-only base URL (no NEXT_PUBLIC_ prefix)
# API_BASE_URL=
